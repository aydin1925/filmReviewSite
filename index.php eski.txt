<?php

// 1. adım: Veritabanını bağlıyorum
require_once 'config/db.php';

// 2. adım: Verileri çekeceğim

try {
    // movies tablosundaki her şeyi getiriyorum
    $sorgu = $db->query("SELECT * FROM movies");

    // Verileri dizi olarak çekme kısmı
    $movies = $sorgu->fetchAll(PDO::FETCH_ASSOC);

}
catch(PDOException $e) {
    // herhangi bir hata olursa ekrana yazan kod
    die("Sorgu hatası: " . $e->getMessage());
}

?>

<!DOCTYPE html>
<html lang="tr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>FilmFlux</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">

    <style>
        .card-img-top {
            height: 350px;
            object-fit: cover;
        }
        .card {
            transition: transform 0.2s , box-shadow 0.2s;
        }
        .card:hover {
            transform: translateY(-5px);
            box-shadow: 0 10px 20px rgba(0,0,0,0,2);
        }
    </style>
</head>
<body class="bg-light">
    
    <!-- Navbar -->
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark mb-5 sticky-top">
        <div class="container">
            <a href="#" class="navbar-brand fw-bold">🎬 FilmFlux</a>
        </div>
    </nav>

    <div class="container">
        <!-- Başlık -->
        <div class="row mb-4">
            <div class="col-12 text-center">
                <h1 class="display-6 fw-bold">Vizyondaki Filmler</h1>
                <p class="text-muted">Veritabanından Canlı veri akışı</p>
            </div>
        </div>
    
        <!-- Film Listesi -->
        <div class="row">

            <?php

            // Veritabanı boşsa kullanıcıya bilgi vereceğiz
            if(empty($movies)):
            ?>

            <div class="alert alert-warning text-center">
                Henüz veritabanına film eklenmemiş. Lütfen phpMyAdmin'den veri ekleyin.
            </div>

            <?php else: ?>


            <?php foreach($movies as $movie):
            ?>

                <!-- 
                   col-md-4: Orta ve büyük ekranlarda 3'lü yan yana (12/4 = 3)
                   col-sm-6: Tablette 2'li yan yana (12/6 = 2)
                   col-12: Telefonda tekli (Tam genişlik)
                -->

                <!-- Bir tane şablon film  kutusu hazırlıyorum -->
                 <div class="col-12 col-sm-6 col-md-4 mb-4">
                    <div class="card h-100 shadow-sm border-0">
                        <!-- PHP'den image linkini alıp buraya ekliyorum -->
                         <img src="<?php echo $movie['image_url']; ?>" class="card-img-top" alt="<?php echo $movie['title']; ?>">

                         <div class="card-body d-flex flex-column">
                            <!-- Filmin ismini ekrana getir -->
                             <h5 class="card-title fw-bold"><?php echo htmlspecialchars($movie['title']); ?></h5>

                            <div class="mb-2">
                                <span class="badge bg-primary"><?php echo htmlspecialchars($movie['category']); ?></span>
                                <span class="badge bg-secondary"><?php echo htmlspecialchars($movie['release_year']); ?></span>
                            </div>

                             <!-- Yönetmen ve kategoriyi ekrana getir -->
                              <p class="card-text text-muted small">
                                <strong>Yönetmen:</strong> <?php echo htmlspecialchars($movie['director']); ?> <br>
                              </p>

                              <!-- Özetin sadece ilk 100 karakterini göstereceğim -->
                               <p class="card-text small">
                                <?php echo htmlspecialchars(substr($movie['description'],0,90)) . '...'; ?>
                               </p>

                              <!-- mt-auto: Butonu her zaman en alta iter -->
                               <a href="detay.php?id=<?php echo $movie['movie_id']; ?>" class="btn btn-dark w-100 mt-auto"> Detayları Gör</a>
                         </div>
                    </div>
                 </div>
            
            <?php
            // Döngüyü bitir.
            endforeach;
            ?>

        <?php
        endif;
        ?>

        </div>

    </div>
    
    <!-- Bootstrap JS (Opsiyonel: Açılır menüler için gerekli) -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>